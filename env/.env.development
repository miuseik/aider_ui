# 开发环境配置
VITE_API_URL=https://localhost:8442
# VITE_WS_URL 留空 → 前端自动跟随浏览器当前访问的域名（访问哪个域名就连哪个域名的 ws，
# 由 vite /ws 代理转发到本机 8442）。如需强制指定 ws 地址（server 不在同源）再取消下一行注释并设置。
# VITE_WS_URL=wss://localhost:8442/ws/client/ui